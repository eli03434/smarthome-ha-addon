# תלויות שנבנו על מחשב הפיתוח (Windows) — הקונטיינר מתקין את שלו ב-npm ci.
# בלי זה, בנייה מקומית הייתה מעתיקה אותן פנימה ודורסת את ההתקנה הנכונה.
rootfs/app/node_modules/
# מצב ריצה מקומי — לא שייך לתמונה (בקונטיינר הוא נשמר ב-/data)
rootfs/app/data/
rootfs/app/server_config.json
